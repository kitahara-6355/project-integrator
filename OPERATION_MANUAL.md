# 統合リポジトリ運用マニュアル

## 1. 新規プロジェクト追加手順（テンプレート）

1.  **ChatGPT-5 に設計図を依頼し、仕様を明確化する。**

2.  **Juelesに以下を指示する：**
    -   新規リポジトリを `services/` にクローン
    -   `config.json` に登録
    -   `run_all.py` に処理フローを追記
    -   CI/CD に組み込み、自動実行を確認。

### 📌 指示テンプレート（Jueles用）
```
services/ に新しい project_xxx を追加してください。
config.json に登録し、run_all.py に処理順を追記してください。
GitHub Actions に反映させてください。
```

## 2. ChatGPT-5 の役割
-   **役割:**
    -   設計図生成、仕様策定
    -   プロジェクト間連携の設計
    -   統合フロー改善提案
-   **成果:**
    -   設計が標準化される
    -   再利用可能なテンプレートが蓄積
    -   知的生産に専念できる

## 3. Jueles の役割
-   **役割:**
    -   Git操作代行（clone, pull, push）
    -   `config.json` / `run_all.py` / CI更新
    -   統合状況のレポート
-   **成果:**
    -   ミスの削減
    -   指示が「型化」され短縮可能
    -   人的負荷を大幅に軽減

## 4. 運用ルール
-   `services/` 配下に配置する（命名規則: `project_xxx`）
-   `config.json` に必ず登録
-   `run_all.py` が唯一の実行入口
-   CI/CD は `run_all.py` を基盤に構築

### 注意点
-   依存関係を明記すること
-   データ形式を統一すること
-   ログを統合的に管理すること
-   疎結合化を意識し、将来拡張を容易にすること

## 5. メリット（総括）
-   **作業の標準化** → どのプロジェクトも同じ流れで統合
-   **指示の省力化** → 「追加・登録・CI更新」で完結
-   **設計の効率化** → ChatGPT-5が全体設計を管理
-   **運用の自動化** → GitHub Actions による自動実行
-   **拡張性** → 新規追加の負荷が一定で、拡張が容易

---
✅ このフローに従えば、あなたの開発スタイルを守りつつ、
-   **Jueles** はオペレーションを
-   **ChatGPT-5** は設計と知的作業を
分担し、あなたは本来の知的生産活動に専念できます。
