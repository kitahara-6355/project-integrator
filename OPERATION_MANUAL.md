# 統合リポジトリ運用マニュアル（あなた専用）

## 1. 既存プロジェクトの取り扱い
### 手順
1. 既存リポジトリ（例: Project A, Project B, …）を `services/` 以下にクローンする。
   - 例: `services/project_a/`, `services/project_b/`
2. 統合リポジトリの `config.json` に追加登録。
3. `run_all.py` に実行順を追記して、統合フローに組み込む。

### メリットと効果
- プロジェクトが一元管理され、依存関係や実行順を見失わない。
- 過去プロジェクトを再利用でき、新規プロジェクト開発のテンプレートにもなる。

## 2. 開発中プロジェクトの取り扱い
### 手順
1. ChatGPT-5で設計図・仕様を策定。
2. Juelesに対し、統合リポジトリへのサブディレクトリ追加と初期コード配置を依頼。
3. 開発中は統合リポジトリの外で試験的に進めてもよいが、完成直前に統合リポジトリに組み込むことを原則とする。

### メリットと効果
- 過去プロジェクトを参照しながら、新しい開発が可能。
- 統合前提の設計により、後工程での統合作業が容易。

## 3. 開発後プロジェクトの取り扱い
### 手順
1. GitHub上の開発リポジトリをクローンして `services/` に取り込み。
2. `config.json`・`run_all.py` に正式登録。
3. CI/CD（GitHub Actions）に統合し、自動実行可能にする。

### メリットと効果
- 運用開始後は「統合フローの一部」として動作し、自動実行・連携が保証される。
- 新規追加も同じ手順なので、拡張が標準化される。

## 4. ChatGPT-5 の役割と期待される成果
### 役割
- 設計図の生成
- プロジェクト間連携の仕様策定
- 統合リポジトリの改善提案

### 期待される成果
- 開発開始前に「全体像」が明確になる。
- 再利用可能なテンプレート設計が蓄積される。
- プロジェクト追加時の 負担を最小化。

## 5. Jueles の役割と期待される成果
### 役割
- 統合リポジトリへのコード取り込み（サブモジュール管理含む）
- `config.json` / `run_all.py` / CI/CD の更新
- GitHubリポジトリ間の同期・クリーンアップ

### 期待される成果
- 煩雑なGit操作を代行
- 人間の操作ミスを最小化
- 指示を「パターン化」でき、毎回短いコマンド風の指示で完結

## 6. プロジェクト間の連携および今後の拡張に関する運用ルール
### 運用ルール
- `services/` 配下に必ず配置する（命名規則: `project_xxx`）。
- `config.json` に必ず登録し、実行順序を管理する。
- `run_all.py` を統合実行の唯一の入り口とする。
- CI/CD（GitHub Actions）は `run_all.py` をベースに構築する。

### 開発者が気を付けるべき点
- 依存関係の明記（例: Project A → Project B の出力依存）
- データ形式の統一（Notion登録用JSONやCSVなど）
- ログ管理の一元化（エラーも統合リポジトリで収集）
- 拡張を見越した設計（新規追加が容易になるよう疎結合化）

## 7. このフローで得られるメリット（総括）
- **作業の標準化**: どのプロジェクトも同じパターンで統合できる
- **指示の省力化**: Juelesへの指示は「追加・登録・CI更新」の3点セットに固定
- **設計の効率化**: ChatGPT-5は「設計・仕様策定・最適化」に専念できる
- **運用の自動化**: `run_all.py` と GitHub Actions により、統合リポジトリ全体が自動稼働
- **拡張性**: 新プロジェクトが増えても、統合の負荷がほぼ一定

---
**💡 要するに：**
- **既存プロジェクト** → 再利用資産化
- **開発中プロジェクト** → 統合前提で設計
- **開発後プロジェクト** → 自動フローに組み込み
- **ChatGPT-5** → 設計支援、知的生産強化
- **Jueles** → 統合作業自動化、Git操作代行

このマニュアルに従えば、あなたの開発スタイルを崩さず、負荷を下げつつ「知的活動に専念」できる流れになります。
